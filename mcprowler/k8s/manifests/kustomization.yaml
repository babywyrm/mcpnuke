apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mcprowler

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - job.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize

images:
  - name: mcprowler
    newTag: latest
